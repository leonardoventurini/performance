{
  "commands": {
    "install": "meteor npm ci",
    "build": "meteor npm run build",
    "start": "meteor npm start"
  },
  "health": {
    "path": "/health"
  },
  "deploy": {
    "rootDirectory": "apps/dashboard"
  }
}
